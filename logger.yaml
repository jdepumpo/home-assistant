default: warning
# logs:
  # homeassistant.components.device_tracker: critical
  # homeassistant.components.zwave: critical
  # sensor_hulkhaugen84: error
  # homeassistant.helpers.entity.sensor_kayeboy: error
  # homeassistant.components.sensor_mbentzen: error
